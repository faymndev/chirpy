DB_URL=postgres://username:password@localhost:5432/chirpy?sslmode=disable

# enables development routes, like resetting the database
PLATFORM=dev

# openssl rand -base64 60 
JWT_SECRET=

# this isn't actually real - it's a mock service provided by Boot.dev's testing suite 
POLKA_API_KEY=
